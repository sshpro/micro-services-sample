spring:
  datasource:
    username: root
    password: password
    url: jdbc:mysql://localhost:3306/albumapp-prod
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: 'true'
  config:
    activate:
      on-profile: prod